version: 0.1
component: build
timeoutInSeconds: 600
runAs: root
shell: bash

steps:
  - type: Command
    name: "Test"
    command: |
      echo step1 printing
      echo step1 printing again
      git fetch origin
      echo Done fetching
      git checkout origin/poc1
      git diff --name-only origin/master...origin/poc1
