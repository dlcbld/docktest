version: 0.1
component: build
timeoutInSeconds: 600
runAs: root
shell: bash

steps:
  - type: Command
    name: "Test"
    command: |
      echo step1 printing
      echo step1 printing again
      
      git remote add origin https://github.com/dlcbld/docktest.git
      git fetch origin
      echo Done fetching
      
      git checkout origin/poc1
      echo Done checking out poc1
      
      git branch
      echo done branching
      
      git diff --name-only master...poc1
