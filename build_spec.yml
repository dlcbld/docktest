version: 0.1
component: build
timeoutInSeconds: 600
runAs: root
shell: bash

steps:
  - type: Command
    name: "Test"
    command: |
      echo step1 printing
      echo step1 printing again
      git fetch --all
      echo Done fetching
      git branch
      echo Done branching
      git checkout poc1
      git diff --name-only master...poc1
